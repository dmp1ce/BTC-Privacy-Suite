# All channels fees according to ratio to auto-balance
# Fees are updated when fee delta >= 120
# Using KISS principle to try and cover all channels with one rule
[default]
strategy = proportional
base_fee_ppm = 0
min_fee_ppm = 0
max_fee_ppm = 1200
min_fee_ppm_delta = 120

# Add exceptions here